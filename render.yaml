services:
  - type: web
    name: e3p-platform
    env: docker
    plan: free
    region: oregon
    healthCheckPath: /
    envVars:
      - key: PORT
        value: 8080
      - key: NODE_ENV
        value: production
